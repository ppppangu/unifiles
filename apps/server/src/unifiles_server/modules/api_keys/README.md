# API keys module

Owns machine-credential creation, listing, scope assignment, and revocation.
The current local credential store is replaced by the external key provider in
the authentication implementation phase.
