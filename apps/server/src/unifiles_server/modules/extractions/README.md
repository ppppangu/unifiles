# Extractions module

Owns extraction lifecycle APIs, built-in text/PDF parsing, and remote OCR
provider integration. Background execution is scheduled by the transport
adapter and performed by service.py.
