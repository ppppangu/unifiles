# 技术交底书

## 一、发明名称

一种基于多模态模型的文档统一处理方法及系统。

## 二、技术领域

本发明属于计算机数据处理技术领域，特别涉及一种将包含文本、图片等多种元素的异构文档（如PDF、Word）进行统一化、结构化处理的方法及系统，可广泛应用于知识库构建、内容管理和智能检索等场景。

## 三、背景技术

在当前信息化建设中，企业和个人积累了大量电子文档，这些文档格式各异（如PDF, Word, PPT, 图片等），且内容复杂，往往包含文本、图表、照片等多种元素（即多模态内容）。

现有技术在处理这些文档时存在以下主要问题：
1.  **信息提取不完整**：传统的文档处理系统（如OCR）主要关注于提取纯文本，对于文档中的图片、图表等非文本元素，通常直接忽略或仅保存为图片文件，导致图片中蕴含的关键信息（如流程图的步骤、统计图的数据）完全丢失。
2.  **处理流程碎片化**：缺乏统一的处理框架，针对不同格式的文档需要开发不同的处理逻辑，系统复杂性高，维护困难。
3.  **内容冗余与不一致**：同一份原始文件在不同的业务场景下（如用于A项目的问答库，又用于B项目的报告分析），往往需要被重复处理和存储，不仅造成存储资源浪费，还可能因处理方式不同导致内容不一致。
4.  **内容利用率低**：提取出的内容与具体应用场景耦合过深，难以实现“一次提取，多次复用”，灵活性差。

因此，如何高效、完整地从多模态文档中提取信息，并以一种统一、灵活的方式进行管理和应用，是本领域亟待解决的技术难题。

## 四、发明内容

### 1. 发明目的

本发明旨在解决现有技术中多模态文档信息提取不完整、处理效率低下、内容复用性差等问题，提供一种能够将多种格式的文档（特别是包含图片的文档）高效、无损地转换为统一纯文本格式进行处理的方法和系统。

### 2. 技术方案

为实现上述目的，本发明提出一种创新的文档处理流水线（Pipeline），其核心思想是**以Markdown作为内容的单一真实来源（Single Source of Truth）**，并通过一个关键的**多模态内容提取阶段**，将所有输入文档统一转换为标准化的Markdown格式。

该方法包括以下步骤：
1.  **接收与验证**：系统接收用户上传的任意格式文件（如PDF, Word, 图片），并进行格式、大小等基础验证。
2.  **统一格式转换**：将各种输入格式（如Word, PPT）统一转换为PDF格式，为后续内容提取做准备。这是一个可选步骤，但有助于简化后续处理。
3.  **核心内容提取（发明关键点）**：
    a.  系统采用一个**多模态大模型**对PDF文件进行深度解析。
    b.  对于文件中的文本部分，直接进行提取。
    c.  对于文件中的**图片、图表等非文本元素**，多模态大模型会对其进行**识别、理解和分析**，并生成一段详尽、准确的**文本描述**。例如，将一张销售趋势图表转换为文字描述：“一张柱状图，显示了2023年四个季度的销售额，Q1为100万，Q2为120万，Q3为150万，Q4为140万。”
    d.  将提取的文本和生成的图片描述，按照原始文档的布局和语义关系，组合成一份**标准化的Markdown文档**。图片本身可被独立存储，并在Markdown中被其文本描述所引用。
4.  **内容存储**：将生成的完整Markdown内容作为“单一真实来源”，存储在数据库的`extracted_contents`表中。原始文件和提取出的图片等资源则存入对象存储（如MinIO）。
5.  **内容应用**：
    a.  当用户创建知识库或进行其他应用时，系统直接引用已存储的Markdown内容，而无需重新处理原始文件。
    b.  根据具体应用场景，对同一份Markdown内容采用不同的分块（Chunking）和向量化（Embedding）策略，生成用于检索的文档块（Chunks），并存入数据库。

本发明还提供了一个实现上述方法的系统，该系统采用分层架构，包括：
-   **应用层**：提供API接口，处理用户请求。
-   **核心层**：包含实现上述文档处理流水线（Pipeline）的核心逻辑。
-   **存储层**：由对象存储（用于存文件）和数据库（用于存元数据、Markdown内容和向量）组成。

### 3. 有益效果

与现有技术相比，本发明具有以下显著优势：
1.  **信息完整性**：通过多模态模型将图片内容转化为文本描述，首次实现了对文档中非文本信息的有效提取和利用，极大减少了信息损失。
2.  **格式统一性**：将所有异构、多模态的输入统一为标准、轻量级的Markdown格式，简化了后续所有处理环节，降低了系统复杂度。
3.  **高效率与高复用性**：实现了“一次提取，多次使用”。标准化的Markdown内容可被不同知识库、不同应用场景反复利用，避免了重复处理原始文件带来的资源消耗。
4.  **高灵活性与可扩展性**：流水线和分层架构的设计使得添加新的文件格式支持、分块算法或嵌入模型变得简单，系统扩展性强。

## 五、附图说明

为了更清楚地说明本发明的实施方式，下面将结合附图进行描述。这些附图是`docs/ARCHITECTURE.md`文档中定义的图表。

-   **图1 (架构分层图)**: 展示了本发明系统的应用层、核心层和存储层的三层分层结构。
-   **图2 (数据处理流程图)**: 详细描述了从文件上传到最终生成可供查询的文档块（Chunks）的完整数据流。
-   **图3 (文档处理流水线图)**: 直观地展示了文档经过验证、转换、OCR（核心内容提取）、分块、向量化和存储的各个阶段。
-   **图4 (数据模型ER图)**: 定义了系统中的核心数据实体及其相互关系，如`files`, `extracted_contents`, `documents`, `chunks`等。

## 六、具体实施方式

下面结合附图，对本发明的具体实施方式进行详细阐述。

本发明的系统架构如**图1**所示，分为应用层、核心层和存储层。

**数据处理的完整流程**如**图2**和**图3**所示，核心是一个异步的文档处理流水线（Pipeline）。

1.  **文件上传与入队**：用户通过应用层（如一个RESTful API）上传一个多模态文件（例如，一个包含公司介绍和产品图片的PDF）。系统在`files`表中创建一条记录，并将该文件加入处理队列。

2.  **流水线处理**：
    -   **Validation (验证)**: 流水线从队列中获取任务，首先验证文件的合法性。
    -   **Conversion (格式转换)**: 若文件为Word等格式，将其转换为PDF。
    -   **OCR (核心内容提取)**: 这是本发明的核心。
        -   一个`DocumentProcessor`控制器调用多模态内容提取服务。
        -   该服务接收PDF文件，利用多模态大模型进行解析。
        -   模型逐页处理，提取文本内容。当遇到一张图片（如产品照片）时，模型会生成描述性文字，例如：“一张展示了XX型号智能手表的白色背景产品图，手表屏幕上显示时间10:10”。
        -   所有提取的文本和图片描述被整合为一个Markdown文件。图片资源本身被上传到MinIO对象存储，其URL可在Markdown中被引用。
        -   这份完整的Markdown内容被保存在`extracted_contents`表中，与原始`files`记录关联。

3.  **知识库构建与内容复用**：
    -   用户可以创建一个或多个知识库（`knowledge_bases`表）。
    -   用户选择将已处理的文件内容添加到一个知识库中。此时，系统会在`documents`表中创建一条记录，该记录**引用**`extracted_contents`表中的Markdown内容，并与目标知识库关联。
    -   关键在于，如果用户想将**同一个文件内容**添加到另一个知识库，只需再创建一条`documents`记录，引用**同一份**`extracted_contents`即可，无需重复处理文件。

4.  **分块与向量化**：
    -   针对每一条`documents`记录，系统可以根据该知识库的特定需求（如用于精确问答或摘要生成），应用不同的分块策略（如按段落、按语义）。
    -   将Markdown内容分割成多个小的文本块（Chunks），并为每个文本块生成向量嵌入（Embedding）。
    -   `chunks`表存储这些文本块、元数据及其向量，用于后续的向量相似度搜索。

**数据模型**如**图4**所示，清晰地体现了本发明的“零冗余”和“高灵活性”设计。`extracted_contents`作为内容的唯一真实来源，被`documents`实体多次引用，每个`documents`实体又可以根据不同策略生成不同的`chunks`集合。

通过以上实施方式，本发明成功地解决的背景技术中提出的问题，实现了对多模态文档的高效、完整的统一处理。
